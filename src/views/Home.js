import React from "react";
import {Text, View, StyleSheet } from "react-native";

const Home = () =>{

    const nome = 'Home Screen';
  
    return(
      <View>
        <Text>{nome}</Text>
      </View>
    );
  
  }

export default Home;