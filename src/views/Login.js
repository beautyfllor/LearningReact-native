import React from "react";
import {Text, View, StyleSheet } from "react-native";

const Login = () =>{

    const nome = 'Login Screen';
  
    return(
      <View>
        <Text>{nome}</Text>
      </View>
    );
  
  }

  export default Login;